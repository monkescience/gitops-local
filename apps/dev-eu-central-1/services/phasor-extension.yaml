apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: phasor-extension
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: phasor
  project: services
  source:
    repoURL: file:///gitops
    targetRevision: HEAD
    path: manifests/phasor-extension/dev-eu-central-1
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
