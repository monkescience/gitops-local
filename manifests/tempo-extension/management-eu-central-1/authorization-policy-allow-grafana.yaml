apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-grafana
  namespace: tempo
spec:
  rules:
    - from:
        - source:
            namespaces:
              - grafana
