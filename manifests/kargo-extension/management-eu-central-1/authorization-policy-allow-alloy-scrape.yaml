apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-alloy-scrape
  namespace: kargo
spec:
  rules:
    - from:
        - source:
            namespaces:
              - alloy
      to:
        - operation:
            ports:
              - "8080"
