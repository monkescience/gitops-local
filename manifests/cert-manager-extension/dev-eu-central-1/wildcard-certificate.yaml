apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: localhost-wildcard
  namespace: istio-system
spec:
  secretName: localhost-wildcard-tls
  dnsNames:
    - "localhost"
    - "*.localhost"
  issuerRef:
    name: localhost-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
